# For the windows network shared drive there is a problem
# with knitr and pandoc. In order to avoid this we set the 
# working directory to a local directory on the hard drive.
setwd("c:/temp/rmd_knit/")

# MAKE SURE to set the knit directory to Current Working
# Directory in the settings for Rstudio.